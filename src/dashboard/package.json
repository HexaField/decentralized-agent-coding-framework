{
  "name": "mvp-dashboard",
  "private": true,
  "dependencies": {
  "express": "^4.19.2",
  "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
  "chai": "^5.1.1",
    "mocha": "^10.7.3",
    "supertest": "^7.0.0"
  },
  "scripts": {
  "lint": "eslint server ui --ext .js",
  "test": "mocha --reporter spec --require ./server/test-setup.js \"server/**/*.test.js\""
  }
}